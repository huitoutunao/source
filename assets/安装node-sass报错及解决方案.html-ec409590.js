import{_ as s,o as d,c as o,d as a}from"./app-138581c8.js";const c={};function n(t,e){return d(),o("div",null,[...e[0]||(e[0]=[a(`<h1 id="安装-node-sass-报错及解决方案" tabindex="-1"><a class="header-anchor" href="#安装-node-sass-报错及解决方案" aria-hidden="true">#</a> 安装 node-sass 报错及解决方案</h1><blockquote><p>原文：https://www.kancloud.cn/uview/uview-ui_v2/2593434</p></blockquote><p>下面是对原文的摘抄：</p><p>这是<code>g++</code>环境报的错，原因是<code>node-sass</code>实际上是个<code>C++</code>模块(不要对此疑惑，<code>Nodejs</code>上的内置模块大多都是<code>C++</code>模块)。通常如果你使用<code>windows</code>系统，你应该安装<code>Visual Studio</code>，如果你使用<code>macos</code>，你应该安装<code>XCode</code>，这两个工具通常都自带完整的编译工具链。</p><p>当然以上仍然不能有效解决问题，因为工具的安装本身就有一点问题，而实际上我们根本不需要编译。</p><p><code>sass</code>有一个编译好的版本名为 <strong>dart-sass</strong> (现已更名为<code>sass</code>)，它有着完全的跨平台兼容性，因为它已经给你编译好了。为了避免库依赖的其他问题，我们最好不要改它的名字，只要改它的内在就可以了。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">npm</span> i node-sass@npm:sass
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这样就可以让它保持<code>node-sass</code>的引用而实际上装的为<code>dart-sass</code>了。</p>`,8)])])}const r=s(c,[["render",n],["__file","安装node-sass报错及解决方案.html.vue"]]);export{r as default};
